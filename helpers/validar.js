const validator = require('validator')

const validarArticulo = (parametros ) => {
    console.log("AQUI SE  VALIDAN LOS DATOS, NO SE USA TRY NI CATCH PERO SI UNA EXEPCION")
    //throw new Error("no se ha validado la informaciòn")
}

module.exports = {
    validarArticulo
}